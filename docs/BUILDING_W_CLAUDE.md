-> ML system (what we know and what do we want to predict?)

-> domain driven design (each module with its explanation)

-> I noticed that you build all the modules unitl now just by making assumptions, without having any gorund truth. Let's then add a notebook in each module to validate the approach and then eventually re-defining the system part.

-> claude code build, chat teaches

-> Python fluency, HOW EVERYTHING FITS TOGETHER? the project can progress just as much as I can validate and understand it though notebooks